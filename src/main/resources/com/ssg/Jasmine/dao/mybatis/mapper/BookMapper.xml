<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssg.Jasmine.dao.mybatis.mapper.BookMapper">
  <cache />
  
  <select id="getBookByBookId" resultType="com.ssg.Jasmine.domain.Book">
    SELECT BOOKID, ISBN, PRICE, TITLE, AUTHOR, PUBLISHER, AUTHOR, CATEGORYID, USERID, IMG, REGISTERDATE
    FROM BOOK 
    WHERE BOOKID = #{bookId}
  </select> 
  

  <insert id="createBook" parameterType="com.ssg.Jasmine.domain.Book">
  
  <selectKey keyProperty="bookId" resultType="int" order="BEFORE">
  	select SEQ_bookID.nextval FROM DUAL
  </selectKey>
  
  	INSERT INTO BOOK 
  	( BOOKID, ISBN, PRICE, TITLE, AUTHOR, PUBLISHER, CATEGORYID, USERID, IMG, REGISTERDATE)
  	VALUES 
  	(#{bookId}, #{isbn}, #{price}, #{title , jdbcType=VARCHAR}, 
  	#{author, jdbcType=VARCHAR}, #{publisher, jdbcType=VARCHAR}, #{categoryId}, #{userId}, #{img}, SYSDATE) 

  </insert>
  
  <update id="updateBook" parameterType="com.ssg.Jasmine.domain.Book">
  	UPDATE BOOK SET
  		ISBN = #{isbn},
  		PRICE = #{address},
  		TITLE = #{phone},
  		AUTHOR ={author},
  		PUBLISHER ={publisher},
  		CATEGORY={category}
  	WHERE BOOKID = #{bookId}
  </update>
  
  <delete id="deleteBook" parameterType="com.ssg.Jasmine.domain.Book">
  	DELETE BOOK 
  	WHERE BOOKID = #{bookId}
  </delete>
  
  <select id="getBookList" resultType="com.ssg.Jasmine.domain.Book">
    SELECT BOOKID, ISBN, PRICE, TITLE, AUTHOR, PUBLISHER, AUTHOR, CATEGORYID, USERID, IMG, REGISTERDATE
    FROM BOOK
    ORDER BY BOOKID DESC
  </select> 
  
   <select id="getBookRecent3" resultType="com.ssg.Jasmine.domain.Book">
   	 <![CDATA[
	    SELECT *
		FROM (SELECT * FROM BOOK ORDER BY bookid DESC)
		WHERE rownum <= 3
	  ]]>
  </select> 
  
    <select id="getBookByUserId" resultType="com.ssg.Jasmine.domain.Book">
    SELECT BOOKID, ISBN, PRICE, TITLE, AUTHOR, PUBLISHER, AUTHOR, CATEGORYID, USERID, IMG
    FROM BOOK 
    WHERE USERID = #{userId}
  </select> 
  
  
  
  <!-- Search auctions by Keyword -->
  <select id="auctionListByKeyword" resultType="com.ssg.Jasmine.domain.Auction">
  	<bind name="pattern" value="'%' + _parameter + '%'" />
	  	SELECT BOOKID, ISBN, PRICE, TITLE, AUTHOR, PUBLISHER, AUTHOR, CATEGORYID, USERID, IMG
    	FROM BOOK
	    WHERE title LIKE #{pattern}
  </select>
  
  
  
  <select id="bookListCnt" resultType="int">
  		SELECT count(*)
  		FROM BOOK
  </select>
  
  <select id="bookList" resultType="com.ssg.Jasmine.domain.Book" parameterType="hashmap">
		
    SELECT BOOKID, ISBN, PRICE, TITLE, AUTHOR, PUBLISHER, AUTHOR, CATEGORYID, USERID, IMG, REGISTERDATE
  FROM (
        SELECT ROW_NUMBER() OVER (ORDER BY bookid desc) NUM
             , A.*
          FROM book A
         ORDER BY bookid desc
        ) 
 WHERE NUM BETWEEN #{pageStart} AND #{perPageNum}
		
		
		
		
		
		
	</select>

  
  
  
  

</mapper>